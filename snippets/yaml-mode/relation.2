# -*- mode: snippet -*-
# name: relation.many-to-many
# key: relation
# --
${1:name}: 
  class: ${2:Other}
  refClass: ${3:ThisOther}
  local: ${4:this_id}
  foreign: ${5:other_id}
  foreignAlias: ${6:Thises}
